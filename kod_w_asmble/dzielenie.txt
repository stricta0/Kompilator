#Z zalozenie
# reg, zero, one, a, b to nasze adresy zmiennych

LOAD b
STORE old_b
JZERO 2
JUMP 3
LOAD 0git
JUMP koniec
LOAD 0
STORE save_power


#Poczatek petli
LOAD 1
STORE power #power = 1
LOAD b
SUB A #reg = b - a
JNEG 8 #if b - a < 0 -> zakoncz petle wewnetrzna (while a < b)
LOAD power
ADD power
STORE power #power = power + power
LOAD b
ADD b
STORE b #b = b + b
JUMP -9
LOAD b
SUB a #reg = b - a
JZERO 2 #if b == a
JUMP 4 #else
LOAD power #then
ADD save_power
JUMP end
LOAD power #else body
HALF #power // 2
STORE power #power = power // 2
ADD save_power #reg = power + save_power
STORE save_power #save_power += power
LOAD a #reg = a
SUB b #reg = a -b
STORE a #a = a - b
LOAD old_b
STORE b
SUB a #b - a > 0
JPOS 2
JUMP #wroc do petli
LOAD save_power
#KONIEC



#poczatek petli 2
